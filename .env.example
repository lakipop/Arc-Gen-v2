# Example environment file for ArcGen
# Copy this to `.env` and replace the placeholders.

# Google Gemini / GenAI API key
VITE_GEMINI_API_KEY="YOUR_GEMINI_API_KEY_GOES_HERE"

# Notes:
# - Do NOT commit your `.env` file to source control. The repo includes `.gitignore` with .env entries.
# - Restart the dev server after updating `.env`.
